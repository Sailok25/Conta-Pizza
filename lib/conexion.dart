import 'package:flutter/material.dart';

class Conexion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Conexión'),
      ),
      body: Center(
        child: Text('Pantalla de Conexión'),
      ),
    );
  }
}
